# PyTorch with CUDA support - install with:
# pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 --index-url https://download.pytorch.org/whl/cu121
torch>=2.5.1
torchvision>=0.20.1
transformers>=5.5.0
accelerate>=1.8.0
sentencepiece>=0.2.0
numpy>=2.2.0

# Performance Optimizations
bitsandbytes>=0.46.1  # 4-bit quantization support
# Optional: flash-attn>=2.0.0  # Uncomment for Flash Attention (requires proper CUDA setup)

# Web Framework  
fastapi>=0.116.0
uvicorn>=0.35.0
python-dotenv>=1.1.0
pytest>=8.3.0
httpx>=0.28.0
